# Static resolver configuration for Site 1's statically-addressed nodes
# (hS1dns itself and rS1). Primary resolver is the local site DNS server;
# HQ's DNS server is the secondary/fallback resolver.
nameserver 10.0.10.2
nameserver 10.0.17.69
search site1.local hq.local
