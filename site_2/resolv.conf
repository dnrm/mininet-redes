# Static resolver configuration for Site 2's statically-addressed nodes
# (hS2dns itself and rS2). Primary resolver is the local site DNS server;
# HQ's DNS server is the secondary/fallback resolver.
nameserver 10.0.13.130
nameserver 10.0.17.69
search site2.local hq.local
